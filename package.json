{
  "name": "hardhat-boilerplate",
  "version": "0.2.0",
  "description": "A boilerplate repository for hardhat project",
  "author": {
    "name": "Sumit Guha",
    "email": "sumit03guha@gmail.com"
  },
  "license": "MIT",
  "scripts": {
    "clean-compile": "hardhat clean && hardhat compile",
    "test": "hardhat test --network localhost",
    "deploy": "echo 'ENTER NETWORK' && read network && hardhat run scripts/deploy.ts --network $network",
    "verify": "echo ENTER NETWORK' && read network && hardhat run verify --network $network"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.12.3",
    "hardhat-contract-sizer": "^2.6.1"
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "keywords": [
    "hardhat",
    "smart-contracts",
    "ethers",
    "boilerplate",
    "ethereum",
    "truffle"
  ]
}
